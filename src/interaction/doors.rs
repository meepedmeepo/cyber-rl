use hecs::Entity;

use crate::State;

//#!  todo: actually implement open and close door.

pub fn open_door(state: &mut State, interactor: Entity, target: Entity) {}

pub fn close_door(state: &mut State, interactor: Entity, target: Entity) {}
